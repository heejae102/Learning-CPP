#include<iostream>
using namespace std; 

/*

<컨테이너 클래스>

array, vector 등

array 직접 구현해보기 

*/

class IntArray
{
private:
	int _lenght = 0;
	int *_data = nullptr;

public:
	//Constructors();
	//Destructors();
	//initialize();
	//reset();
	//resize();
	//insertBefore(const int &value, const int &idx);
	//remove(const int &idx);
	//push_back(const int &value);

};

int main()
{
	// IntArray my_arr{1, 3, 5, 7, 9};
	// my_arr.insertBefore(10, 1);			// 1, 10, 3, 5, 7, 9
	// my_arr.remove(3);					// 1, 10, 5, 7, 9
	// my_arr.push_back(13);				// 1, 10, 5, 7, 9, 13

	return 0;
}